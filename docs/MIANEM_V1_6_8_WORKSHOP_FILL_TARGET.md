# Mianem v1.6.8 — Workshop fill target

Goal: do not stop the Workshop after one available result when the first semantic shortlist is mostly registered.

Rules:
- semantic quality stays first; availability never creates or boosts a weak name,
- the backend exposes a deeper curated semantic pool by default,
- the UI still shows only currently available `.com` combinations by default,
- target surface is up to 6–8 strong available combinations when the curated pool can supply them,
- if the deeper pool still contains fewer available names, show the honest smaller result set rather than inventing low-quality combinations,
- full brand screening remains deferred until the user selects a candidate.
